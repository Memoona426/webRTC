<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Room <%= roomId %></title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-900 text-gray-100 h-screen p-4">
  <div class="grid grid-cols-3 grid-rows-2 gap-4 h-full">
    <div id="video-grid" class="col-span-2 row-span-2 border border-gray-700 p-4 rounded bg-gray-800 shadow overflow-auto">
    </div>

    <div id="messages" class="bg-gray-800 border border-gray-700 rounded p-4 overflow-y-auto shadow">
      <div class="flex items-center gap-2">
        <input id="chat_input" placeholder="Type message..." class="flex-grow px-4 py-2 border border-gray-600 rounded bg-gray-700 text-white shadow focus:outline-none focus:ring-2 focus:ring-blue-500" />
        <button onclick="sendMessage()" class="bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-700 transition">
          Send
        </button>
      </div>
    </div>

    <div class="flex gap-4 flex-col">
      <!-- Mic Button -->
      <button id="mic-btn" onclick="toggleMic()" class="bg-red-600 text-white px-4 py-2 rounded hover:bg-red-700 transition">
        <span id="mic-status">Mute</span>
      </button>
      

      <button id="camera-btn" onclick="toggleCamera()" class="bg-green-600 text-white px-4 py-2 rounded hover:bg-green-700 transition">
        <span id="camera-status">Camera Off</span>
      </button>
    </div>
  </div>

  <script src="/socket.io/socket.io.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/peerjs@1.3.1/dist/peerjs.min.js"></script>
  <script>
    const roomId = "<%= roomId %>";
  </script>
  <script src="/js/main.js"></script>
</body>

</html>
